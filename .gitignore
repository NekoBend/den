# caches and environments
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
*.egg-info/
dist/

# den's own workspace memory/hooks when dogfooding the den CLI here
.den/

# agents/.private/ manages its own exclusions via agents/.private/.gitignore.
